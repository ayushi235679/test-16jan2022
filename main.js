function product(){
    window.location="product.html";
}

//you have to design the UI only, below code is additional.

/*function plus()
{
let value = parseInt(document.getElementById('quant').value, 10);
    value = isNaN(value) ? 0 : value;
    if(value<10){
        value++;
            document.getElementById('quant').value = value;
            let sum=parseInt(549*value);
            console.log(sum);
            document.getElementById('total-price').innerHTML = sum;   
    }
}
function minus()
{
    let value = parseInt(document.getElementById('quant').value, 10);
    let price=parseInt(document.getElementById('total-price'));
    value = isNaN(value) ? 0 : value;
    if(value>=1){
        value--;
            document.getElementById('quant').value = value;
            for(i=value;i>=0;i--){
                
                let sum=parseInt(price-549);
            console.log(sum);
            document.getElementById('total-price').innerHTML = sum; 
            }
    }

}*/

